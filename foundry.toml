[profile.default]
src = "src"
out = "out"
libs = ["lib"]
remappings=["@openzeppelin=lib/openzeppelin-contracts","@chainlink=lib/chainlink-brownie-contracts"]

[invariant]
runs=128
depth=128 #no of recursive calls in a single run
fail_on_revert=true